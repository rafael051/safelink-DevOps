package br.com.fiap.safelink.model;

/**
 * # 🎭 Enum: UserRole
 *
 * Define os papéis possíveis que um usuário pode assumir dentro da aplicação SafeLink.
 *
 * - `ADMIN`: tem acesso total, inclusive a recursos administrativos.
 * - `USER`: acesso limitado às funções básicas e de consulta.
 */
public enum UserRole {
    ADMIN,
    USER
}
